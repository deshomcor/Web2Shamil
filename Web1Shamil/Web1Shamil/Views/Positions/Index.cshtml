@model IEnumerable<Web1Shamil.Models.Classes.Positions>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PositionsName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PositionsName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PositionsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PositionsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PositionsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
