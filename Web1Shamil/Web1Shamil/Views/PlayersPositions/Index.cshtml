@model IEnumerable<Web1Shamil.Models.Classes.PlayersPositions>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Players)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Positions)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Players.PlayersName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Positions.PositionsName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlayersPositionsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlayersPositionsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlayersPositionsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
